let a=5;
let  b="5";

if(a===b)
  console.log('bothh are same');
else 
   console.log("not same");

   // == loose comparison means does not care about data type.
   // === strict comparison it cares data type.