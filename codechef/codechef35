// Debug/Fix the given function

function solve(num1, num2) {
  let isEqual = "Equal";
  if(num1 === num2) {
     isEqual = "Equal";
  } else {
     isEqual = "Not Equal";
  }
  console.log(isEqual);
}
